export interface Cv {
  header: Header;
}

export interface Header {
  name: string;
  title: string;
  kaizen: string;
  location: string;
  phone: string;
  email: string;
  web: string;
  headline: string;
}
